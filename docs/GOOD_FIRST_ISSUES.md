# Good First Issues

1. Add spatial grid for interest management in Go server (docs/AUTHORITY_REPLICATION.md)
2. Implement per-client last-ack cache and delta compression
3. Add unit tests for chat moderation and rate-limit
4. Add Python packaging to run client via `pipx` (CLI: `creator-studio`)
5. Add GitHub Pages for docs/ with mkdocs
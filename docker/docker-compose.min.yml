version: '3.8'
services:
  go-server:
    build:
      context: ../go
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    environment:
      - SERVER_PORT=8080
      - MAX_PLAYERS=100
    restart: unless-stopped
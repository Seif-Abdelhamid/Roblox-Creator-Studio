# Bench Results

Client tick (headless):

```
ticks=0 elapsed_s=19.009 ticks_per_sec=0.0
```

WS throughput (local, small sample):

```
To run: start server (docker-compose.min.yml) and install websocket-client for Python.
Example expected output format:
clients=2 messages=10 total=20 elapsed_s=0.50 msgs_per_s=40.0
```